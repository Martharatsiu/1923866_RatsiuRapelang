/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package padsforher.app;

import padsforher.list.LinkedList;

import static padsforher.utilities.StringUtils.join;
import static padsforher.utilities.StringUtils.split;
import static padsforher.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
